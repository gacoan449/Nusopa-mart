<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Theme.Light.NoActionBar is no longer available in newer Android SDKs.
         Use the Material framework theme, which is supported from API 21+. -->
    <style name="LaunchTheme" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>

    <style name="NormalTheme" parent="android:style/Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@android:color/white</item>
    </style>
</resources>
